"""util functions especially for plotting/printing in notebook"""

import matplotlib.pyplot as plt

def check_heatmap_and_paf(image, heatmap, paf): #TODO
    """to check data preprocessing regarding heatmap and PAF generation
    Args:
        image: (np.array) original image
        heatmap: (np.array) heatmap image
        paf: (np.array) PAF image
    """
    
    pass